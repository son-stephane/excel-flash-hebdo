# Dependances du projet.
#
# Installation complete, dependances ET paquet `rapports` :
#     pip install -e ".[dev]"
#
# Ou, si l'installation doit passer par un miroir interne, les DEUX commandes :
#     pip install -r requirements.txt
#     pip install -e . --no-deps

pandas>=2.0        # lecture des fichiers Excel, filtrage, agregations
openpyxl>=3.1      # ecriture du classeur de sortie et de son graphique
matplotlib>=3.8    # graphique du rapport HTML
jinja2>=3.1        # gabarit HTML
